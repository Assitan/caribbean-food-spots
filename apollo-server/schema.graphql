# Included scalars
scalar JSON
scalar Upload

type Spot {
  name: String!
  address: Store!
  phoneNumber: String!
  position:Position!
  rating: Int
  type: String
  tags: [String]
}

type Store {
  name: String
  address: String
  desc: String
  email: String
  datetime: String
}

type Position {
  lat: String!
  lng: String!
}
